#include <stdio.h>
#include <stdlib.h>

#ifndef _FUNCS_H_
#define _FUNCS_H_

#define NUM_COUNT 3 // how many numbers is going to be the input
void GetNumbers(double *arr);
double GetBiggestNumber(double *arr);
void PrintNumbers(double *arr);

#endif